{
  "name": "elearning-fullstack",
  "private": true,
  "scripts": {
    "dev": "concurrently -n server,client -c cyan,magenta -p \"{time} {name}\" -t \"HH:mm:ss.SSS\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
